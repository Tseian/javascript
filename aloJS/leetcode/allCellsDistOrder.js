/**
 * a**2 + b**2 = c**2;
 * @param {number} R
 * @param {number} C
 * @param {number} r0
 * @param {number} c0
 * @return {number[][]}
 */
var allCellsDistOrder = function (R, C, r0, c0) {
    
    for (let i = 0; i < R; i++) {
        for (let j = 0; j < C; j++) {

        }
    }
};